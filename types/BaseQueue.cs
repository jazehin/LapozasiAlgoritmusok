namespace LapozasiAlgoritmusok.types
{
    internal abstract class BaseQueue
    {
        protected int[] _array;
        protected int[] _order; //the order in which elements are stored e.g. [2, 3, 4, 1]
    }
}
